name: Main Pipeline
on: 
  workflow_dispatch:
  push: 
    branches:
      - "main"

jobs:
  Unittest:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: "Lanzar test unitatios"
      run: |
        set -ev
        cd modern-web-app
        npm ci 
        npm run test:unit
      
